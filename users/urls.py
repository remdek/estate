from django.urls import path

from .views import *

app_name = 'users'

#/api/users/

urlpatterns = [
     # path('', UserListView.as_view(), name='UsersListView'),
     # path('top-menu-links/', topMenuLinksView.as_view(), name='topMenuLinks-view'),
]